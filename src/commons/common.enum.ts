export enum userGender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum userStatus {
  NOTACTIVE = 'Not Active',
  ACTIVE = 'Active',
  REMOVED = 'Removed',
}

export enum userRole {
  ADMIN = 'admin',
  MEMBER = 'member',
}
